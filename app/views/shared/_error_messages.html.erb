<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      入力に <%= pluralize(object.errors.count, "個エラー") %>があります。
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li>原稿入力してください。</li>
    <% end %>
    </ul>
  </div>
<% end %>